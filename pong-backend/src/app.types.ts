// The info we can show others about a given user
export interface UserPublic {
  id: number;
  id42: number
  name: string;
  avatar: string;
  games: number;
  wins: number;
  realAvatar: boolean;
}
